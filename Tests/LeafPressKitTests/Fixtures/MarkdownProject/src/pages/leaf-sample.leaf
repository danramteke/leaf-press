sampleProperty: sampleValue
title: Pure Leaf Template Sample

---
#extend("page.leaf"):
#export("content"):
<h1>Sample Leaf Page</h1>

<p>Leaf templates are pure leaf!</p>

<p>The data at the top of the document is passed into the template. Access the property with <code>current.metadata.sampleProperty</code>.
For example, the sample property has a value of #(current.metadata.sampleProperty)</p>

#endexport
#endextend
