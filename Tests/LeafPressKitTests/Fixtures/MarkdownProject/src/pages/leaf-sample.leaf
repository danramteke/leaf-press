sampleProperty: sampleValue
title: Pure Leaf Template Sample

---
#extend("page.leaf")
#export("content"):
<h1>Sample Leaf Page</h1>

<p>Leaf templates are pure leaf! A <code>template</code> set in the front matter is undefined.

<p>The front matter is passed into the template. Access the property with <code>current.frontMatter.sampleProperty</code>. For example, the sample property has a value of #(current.frontMatter.sampleProperty)</p>

#endexport
#endextend
